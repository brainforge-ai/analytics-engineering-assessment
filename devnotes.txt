stg_ 
  - Clean
  - Dedupe
  - Null Handling
  - Date conformity


Mart
  - Incremental Load (Merge)
  --> Fact: Monthly Revenue 
    - Grain - Country x month

Metrcs
  Rolling 30 day (Lag, Rows Window)
  Month-over-Month Growth (abs. / %growth)
  Cohort Analysis

Tests
  - Sanity
  - Freshness
  - Relationship
  

DUCKDB
dbt

RUN generate_seed_data.py

Data quality issues added:
  - ~6% bad dates (invalid, future, very old)
  - ~10% pending orders with null amounts
  - ~30% cancelled orders with near-zero amounts
  - Some duplicate emails in customers
  - One duplicate order_id with different data
  - Added updated_at for incremental logic

~~~ DUCKDB THINGS ~~~
- list tables: 
Duckdb synbtax -->  $SHOW ALL TABLES
Typical SQL vv
  SELECT table_name, table_schema 
  FROM information_schema.tables;

~~~               ~~~


~~~ DBT THINGS ~~~
Reconfirm 'ref's passthrough from seeds to staging

dbt seed is reliably creating physical tables under _raw
dbt run deployed view definitions to _staging schema as stg_<name>

dbt docs generate -- created catalog.json under target directory
database view's seed vs source table seems to distinguish the known csv from that csv as loaded to _raw schema


Regarding schema.yml data-type...

Supported Data Types
dbt supports standard SQL data types, though the exact string you use depends on your data platform (e.g., Snowflake, BigQuery, Postgres). Common categories include:
Numeric: int, integer, decimal, float, numeric.
String: string, varchar, text, char.
Date/Time: date, datetime, timestamp, time.
Boolean: boolean.
Complex/Semi-structured: json, array, variant.

Confirmed addition of schema.yml to seeds directory. Note that example sql undefined table name is a known bug https://github.com/dbt-labs/dbt-docs/issues/407

~~~            ~~~